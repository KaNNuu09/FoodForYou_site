import React from "react";
import { Header } from "../components";

const Main = () => {
  return (
    <main className="flex flex-col items-center justify-center w-screen min-h-screen bg-primary">
      <Header />
    </main>
  );
};

export default Main;
